<div class="{container_classes}">
    {editor_switch}
    <label for="{inputid}" class="form-label">{label}</label>
    <textarea class="form-control {editor_classes}" id="{inputid}" name="{input_name}" id="{inputid}">{input_value}</textarea>
</div>