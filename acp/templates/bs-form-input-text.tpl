<div class="{container_class}">
    <label for="{inputid}" class="form-label">{label}</label>
    {input_group_start}
    {input_group_start_text}
    <input type="{type}" class="{input_classes}" id="{inputid}" name="{input_name}" value="{input_value}">
    {input_group_end_text}
    {input_group_end}
    {form_text}
</div>