<div class="{container_class}">
    <label for="{selectid}" class="form-label">{label}</label>
    <select class="form-select custom-select" name="{select_name}" id="{select_id}">
        {options}
    </select>
</div>