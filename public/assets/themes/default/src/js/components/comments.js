"use strict";

/**
 * Comments component entry. CSS-only - no dedicated JS behaviour yet.
 * Loaded only when the "comments" theme component is enabled for a page.
 */

import '../../scss/components/comments.scss';
