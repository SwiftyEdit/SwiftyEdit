"use strict";

/**
 * Events component entry. CSS-only - no dedicated JS behaviour yet.
 * Loaded only when the "events" theme component is enabled for a page.
 */

import '../../scss/components/events.scss';
