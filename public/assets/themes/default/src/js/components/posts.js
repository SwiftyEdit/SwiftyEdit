"use strict";

/**
 * Posts (blog) component entry. CSS-only - no dedicated JS behaviour yet.
 * Loaded only when the "posts" theme component is enabled for a page.
 */

import '../../scss/components/posts.scss';
